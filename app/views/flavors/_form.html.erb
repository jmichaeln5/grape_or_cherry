<br>


<%= form_with(model: flavor, local: true) do |form| %>
  <% if flavor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(flavor.errors.count, "error") %> prohibited this flavor from being saved:</h2>

      <ul>
      <% flavor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :grape %>
    <%= form.text_field :grape, id: :flavor_grape %>
  </div>

  <div class="field">
    <%= form.label :cherry %>
    <%= form.text_field :cherry, id: :flavor_cherry %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
